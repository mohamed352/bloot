Bloot v1.0.0 (build 16) — QA Release

Fixes from tester feedback (builds 9-11):
• Game invitation: Join button can no longer get stuck loading forever
• Live shows: "no access to watch this game" error fixed (Firestore rules deployed) and watching a just-started game now retries instead of failing
• Public rooms no longer disappear when the game starts — in-progress games stay in the list with a LIVE badge and a Spectate button
